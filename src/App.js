import React, { useEffect, useState } from 'react';
import { getTicketIds } from './services/tvApi';

export const App = () => {
  const [ticketIds, setTicketIds] = useState([]);

  return <p>Hello</p>;
};
